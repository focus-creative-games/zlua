diff --git a/src/lapi.c b/src/lapi.c
index 27fa524..fdf2c62 100644
--- a/src/lapi.c
+++ b/src/lapi.c
@@ -86,6 +86,16 @@ static TValue *index2value (lua_State *L, int idx) {
 }
 
 
+#if ZLUA_FAST_METATABLE
+/*
+** Expose index2value to zlua_fastmt.c without duplicating the implementation
+** (keeps multi-version Lua ports in sync with PUC-Rio lapi.c).
+*/
+TValue *zlua_index2value (lua_State *L, int idx) {
+  return index2value(L, idx);
+}
+#endif
+
 
 /*
 ** Convert a valid actual index (not a pseudo-index) to its address.
diff --git a/src/lgc.c b/src/lgc.c
index c64d74b..7b01ad2 100644
--- a/src/lgc.c
+++ b/src/lgc.c
@@ -607,6 +607,12 @@ static int getmode (global_State *g, Table *h) {
 
 static l_mem traversetable (global_State *g, Table *h) {
   markobjectN(g, h->metatable);
+#if ZLUA_FAST_METATABLE
+  if (h->zlua_mt_kind != 0) {
+    markvalue(g, &h->zlua_index);
+    markvalue(g, &h->zlua_newindex);
+  }
+#endif
   switch (getmode(g, h)) {
     case 0:  /* not weak */
       traversestrongtable(g, h);
diff --git a/src/lobject.h b/src/lobject.h
index 156c942..56c777c 100644
--- a/src/lobject.h
+++ b/src/lobject.h
@@ -777,11 +777,18 @@ typedef struct Table {
   CommonHeader;
   lu_byte flags;  /* 1<<p means tagmethod(p) is not present */
   lu_byte lsizenode;  /* log2 of number of slots of 'node' array */
+#if ZLUA_FAST_METATABLE
+  lu_byte zlua_mt_kind;  /* 0 = normal; non-0 = ZLua sealed IMT/SMT */
+#endif
   unsigned int asize;  /* number of slots in 'array' array */
   Value *array;  /* array part */
   Node *node;
   struct Table *metatable;
   GCObject *gclist;
+#if ZLUA_FAST_METATABLE
+  TValue zlua_index;     /* cached __index (table) */
+  TValue zlua_newindex;  /* cached __newindex (table) */
+#endif
 } Table;
 
 
diff --git a/src/ltable.c b/src/ltable.c
index b7f88f6..11415ff 100644
--- a/src/ltable.c
+++ b/src/ltable.c
@@ -804,6 +804,11 @@ Table *luaH_new (lua_State *L) {
   t->array = NULL;
   t->asize = 0;
   setnodevector(L, t, 0);
+#if ZLUA_FAST_METATABLE
+  t->zlua_mt_kind = 0;
+  setnilvalue(&t->zlua_index);
+  setnilvalue(&t->zlua_newindex);
+#endif
   return t;
 }
 
diff --git a/src/luaconf.h b/src/luaconf.h
index 96a7780..48844fc 100644
--- a/src/luaconf.h
+++ b/src/luaconf.h
@@ -739,6 +739,16 @@
 ** without modifying the main part of the file.
 */
 
+/*
+@@ ZLUA_FAST_METATABLE controls ZLua member-index optimization (see Docs/OPTIMIZATION.md).
+** 0 (default): legacy C __index/__newindex (DispatchInstanceIndex / DispatchInstanceNewIndex).
+** 1: sealed mt + member/setter tables + VM finishget/finishset fast path.
+** Must be the same when compiling Lua core and zlua runtime (affects Table layout).
+*/
+#if !defined(ZLUA_FAST_METATABLE)
+#define ZLUA_FAST_METATABLE 1
+#endif
+
 
 
 #endif
diff --git a/src/lvm.c b/src/lvm.c
index c70e2b8..b183d93 100644
--- a/src/lvm.c
+++ b/src/lvm.c
@@ -30,6 +30,7 @@
 #include "ltable.h"
 #include "ltm.h"
 #include "lvm.h"
+#include "zlua_fastmt.h"
 
 
 /*
@@ -292,6 +293,49 @@ lu_byte luaV_finishget (lua_State *L, const TValue *t, TValue *key,
                                       StkId val, lu_byte tag) {
   int loop;  /* counter to avoid infinite loops */
   const TValue *tm;  /* metamethod */
+#if ZLUA_FAST_METATABLE
+  {
+    Table *mt = NULL;
+    if (ttisfulluserdata(t))
+      mt = uvalue(t)->metatable;
+    else if (ttistable(t))
+      mt = hvalue(t)->metatable;
+    if (mt != NULL && mt->zlua_mt_kind != 0) {
+      Table *idx = hvalue(&mt->zlua_index);
+      TValue slotv;
+      lu_byte slot_tag = luaH_get(idx, key, &slotv);
+      if (!tagisempty(slot_tag)) {
+        if (zlua_is_getter(&slotv))
+          zlua_vm_call_getter(L, &slotv, t, val);
+        else
+          setobj2s(L, val, &slotv);
+        return ttypetag(s2v(val));
+      }
+      /* Static type table: extras such as _default live on SMT itself. */
+      if (ttistable(t)) {
+        lu_byte extra_tag = luaH_get(mt, key, s2v(val));
+        if (!tagisempty(extra_tag))
+          return extra_tag;
+      }
+      /* Reserved keys (__byval_instance_mt, __fullname, ...) stay nil when absent. */
+      if (ttisstring(key)) {
+        TString *ks = tsvalue(key);
+        const char *s = getstr(ks);
+        if (tsslen(ks) >= 2 && s[0] == '_' && s[1] == '_') {
+          setnilvalue(s2v(val));
+          return LUA_VNIL;
+        }
+      }
+      {
+        const TValue *tn = luaH_Hgetshortstr(mt, luaS_newliteral(L, "__zlua_typename"));
+        const char *typeName = (tn != NULL && !isempty(tn) && ttisstring(tn))
+                                   ? getstr(tsvalue(tn)) : "?";
+        const char *memberName = ttisstring(key) ? getstr(tsvalue(key)) : "?";
+        luaG_runerror(L, "zlua: member not found: %s.%s", typeName, memberName);
+      }
+    }
+  }
+#endif
   for (loop = 0; loop < MAXTAGLOOP; loop++) {
     if (tag == LUA_VNOTABLE) {  /* 't' is not a table? */
       lua_assert(!ttistable(t));
@@ -334,6 +375,37 @@ lu_byte luaV_finishget (lua_State *L, const TValue *t, TValue *key,
 void luaV_finishset (lua_State *L, const TValue *t, TValue *key,
                       TValue *val, int hres) {
   int loop;  /* counter to avoid infinite loops */
+#if ZLUA_FAST_METATABLE
+  {
+    Table *mt = NULL;
+    if (ttisfulluserdata(t))
+      mt = uvalue(t)->metatable;
+    else if (ttistable(t))
+      mt = hvalue(t)->metatable;
+    if (mt != NULL && mt->zlua_mt_kind != 0) {
+      Table *setters = hvalue(&mt->zlua_newindex);
+      TValue slotv;
+      lu_byte slot_tag = luaH_get(setters, key, &slotv);
+      if (!tagisempty(slot_tag) && zlua_is_setter(&slotv)) {
+        zlua_vm_call_setter(L, &slotv, t, val);
+        return;
+      }
+      {
+        /* idx lookup only on failure: distinguish read-only vs missing member. */
+        Table *idx = hvalue(&mt->zlua_index);
+        TValue idxv;
+        lu_byte idx_tag = luaH_get(idx, key, &idxv);
+        const TValue *tn = luaH_Hgetshortstr(mt, luaS_newliteral(L, "__zlua_typename"));
+        const char *typeName = (tn != NULL && !isempty(tn) && ttisstring(tn))
+                                   ? getstr(tsvalue(tn)) : "?";
+        const char *memberName = ttisstring(key) ? getstr(tsvalue(key)) : "?";
+        if (!tagisempty(idx_tag))
+          luaG_runerror(L, "zlua: property is read-only: %s.%s", typeName, memberName);
+        luaG_runerror(L, "zlua: member not writable: %s.%s", typeName, memberName);
+      }
+    }
+  }
+#endif
   for (loop = 0; loop < MAXTAGLOOP; loop++) {
     const TValue *tm;  /* '__newindex' metamethod */
     if (hres != HNOTATABLE) {  /* is 't' a table? */
diff --git a/src/zlua_fastmt.c b/src/zlua_fastmt.c
new file mode 100644
index 0000000..2207020
--- /dev/null
+++ b/src/zlua_fastmt.c
@@ -0,0 +1,83 @@
+/*
+** ZLua sealed-metatable support for PUC-Rio Lua 5.5 VM.
+*/
+#define LUA_CORE
+
+#include "lprefix.h"
+
+#include "lua.h"
+#include "lauxlib.h"
+
+#include "lapi.h"
+#include "ldo.h"
+#include "lfunc.h"
+#include "lgc.h"
+#include "lobject.h"
+#include "lstate.h"
+#include "ltable.h"
+#include "ltm.h"
+#include "zlua_fastmt.h"
+
+#if ZLUA_FAST_METATABLE
+
+/* Defined in lapi.c -- thin wrapper around static index2value. */
+TValue *zlua_index2value (lua_State *L, int idx);
+
+LUA_API void zlua_mt_seal (lua_State *L, int mtIndex) {
+  Table *mt;
+  const TValue *idx;
+  const TValue *nidx;
+  TValue *o;
+  lua_lock(L);
+  o = zlua_index2value(L, mtIndex);
+  api_check(L, ttistable(o), "table expected");
+  mt = hvalue(o);
+  idx = luaH_Hgetshortstr(mt, G(L)->tmname[TM_INDEX]);
+  nidx = luaH_Hgetshortstr(mt, G(L)->tmname[TM_NEWINDEX]);
+  if (l_unlikely(isempty(idx) || !ttistable(idx))) {
+    lua_unlock(L);
+    luaL_error(L, "zlua_mt_seal: __index must be a table");
+  }
+  if (l_unlikely(isempty(nidx) || !ttistable(nidx))) {
+    lua_unlock(L);
+    luaL_error(L, "zlua_mt_seal: __newindex must be a table");
+  }
+  setobj(L, &mt->zlua_index, idx);
+  setobj(L, &mt->zlua_newindex, nidx);
+  luaC_barrierback(L, obj2gco(mt), idx);
+  luaC_barrierback(L, obj2gco(mt), nidx);
+  mt->zlua_mt_kind = 1;
+  lua_unlock(L);
+}
+
+
+LUA_API void zlua_vm_call_getter (lua_State *L, const TValue *f,
+                                  const TValue *obj, StkId res) {
+  ptrdiff_t result = savestack(L, res);
+  StkId func = L->top.p;
+  setobj2s(L, func, f);
+  setobj2s(L, func + 1, obj);
+  L->top.p += 2;
+  if (isLuacode(L->ci))
+    luaD_call(L, func, 1);
+  else
+    luaD_callnoyield(L, func, 1);
+  res = restorestack(L, result);
+  setobjs2s(L, res, --L->top.p);
+}
+
+
+LUA_API void zlua_vm_call_setter (lua_State *L, const TValue *f,
+                                  const TValue *obj, const TValue *val) {
+  StkId func = L->top.p;
+  setobj2s(L, func, f);
+  setobj2s(L, func + 1, obj);
+  setobj2s(L, func + 2, val);
+  L->top.p += 3;
+  if (isLuacode(L->ci))
+    luaD_call(L, func, 0);
+  else
+    luaD_callnoyield(L, func, 0);
+}
+
+#endif /* ZLUA_FAST_METATABLE */
diff --git a/src/zlua_fastmt.h b/src/zlua_fastmt.h
new file mode 100644
index 0000000..486fb89
--- /dev/null
+++ b/src/zlua_fastmt.h
@@ -0,0 +1,35 @@
+/*
+** ZLua sealed-metatable helpers (see Docs/OPTIMIZATION.md).
+** Only active when ZLUA_FAST_METATABLE != 0 (defined in luaconf.h).
+*/
+#ifndef zlua_fastmt_h
+#define zlua_fastmt_h
+
+#include "luaconf.h"
+#include "lua.h"
+#include "lobject.h"
+
+#if ZLUA_FAST_METATABLE
+
+/* Tagged field/property accessors; VM compares CClosure->f to these. */
+LUA_API int zlua_index_getter (lua_State *L);  /* (obj) -> value */
+LUA_API int zlua_index_setter (lua_State *L);  /* (obj, value) -> 0 */
+
+/* After __index/__newindex tables are set on mt, cache them and mark sealed. */
+LUA_API void zlua_mt_seal (lua_State *L, int mtIndex);
+
+/* VM helpers: call tagged closures without going through __index. */
+LUA_API void zlua_vm_call_getter (lua_State *L, const TValue *f,
+                                  const TValue *obj, StkId res);
+LUA_API void zlua_vm_call_setter (lua_State *L, const TValue *f,
+                                  const TValue *obj, const TValue *val);
+
+#define zlua_is_getter(o) \
+  (ttisCclosure(o) && clCvalue(o)->f == zlua_index_getter)
+
+#define zlua_is_setter(o) \
+  (ttisCclosure(o) && clCvalue(o)->f == zlua_index_setter)
+
+#endif /* ZLUA_FAST_METATABLE */
+
+#endif
