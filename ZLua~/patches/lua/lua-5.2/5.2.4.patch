From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: zlua-regen <zlua@code-philosophy.com>
Date: Wed, 12 Aug 2026 17:50:00 +0800
Subject: [PATCH] FastMT for lua-5.2.4 (floor)

diff --git a/src/lapi.c b/patched/src/lapi.c
--- a/src/lapi.c
+++ b/src/lapi.c
@@ -79,6 +79,16 @@ static TValue *index2addr (lua_State *L, int idx) {
 }
 
 
+#if ZLUA_FAST_METATABLE
+/*
+** Expose index2addr to zlua_fastmt.c without duplicating the implementation.
+*/
+TValue *zlua_index2value (lua_State *L, int idx) {
+  return index2addr(L, idx);
+}
+#endif
+
+
 /*
 ** to be called by 'lua_checkstack' in protected mode, to grow stack
 ** capturing memory errors
diff --git a/src/lgc.c b/patched/src/lgc.c
--- a/src/lgc.c
+++ b/src/lgc.c
@@ -435,6 +435,12 @@ static lu_mem traversetable (global_State *g, Table *h) {
   const char *weakkey, *weakvalue;
   const TValue *mode = gfasttm(g, h->metatable, TM_MODE);
   markobject(g, h->metatable);
+#if ZLUA_FAST_METATABLE
+  if (h->zlua_mt_kind != 0) {
+    markvalue(g, &h->zlua_index);
+    markvalue(g, &h->zlua_newindex);
+  }
+#endif
   if (mode && ttisstring(mode) &&  /* is there a weak mode? */
       ((weakkey = strchr(svalue(mode), 'k')),
        (weakvalue = strchr(svalue(mode), 'v')),
diff --git a/src/llimits.h b/patched/src/llimits.h
--- a/src/llimits.h
+++ b/src/llimits.h
@@ -276,7 +276,7 @@ union luai_Cast { double l_d; LUA_INT32 l_p[2]; };
 
 
 
-#if defined(ltable_c) && !defined(luai_hashnum)
+#if !defined(luai_hashnum) /* ZLua: Il2Cpp lump-safe */
 
 #include <float.h>
 #include <math.h>
diff --git a/src/lobject.h b/patched/src/lobject.h
--- a/src/lobject.h
+++ b/src/lobject.h
@@ -561,12 +561,19 @@ typedef struct Table {
   CommonHeader;
   lu_byte flags;  /* 1<<p means tagmethod(p) is not present */
   lu_byte lsizenode;  /* log2 of size of `node' array */
+#if ZLUA_FAST_METATABLE
+  lu_byte zlua_mt_kind;  /* 0 = normal; non-0 = ZLua sealed IMT/SMT */
+#endif
   int sizearray;  /* size of `array' array */
   TValue *array;  /* array part */
   Node *node;
   Node *lastfree;  /* any free position is before this position */
   struct Table *metatable;
   GCObject *gclist;
+#if ZLUA_FAST_METATABLE
+  TValue zlua_index;     /* cached __index (table) */
+  TValue zlua_newindex;  /* cached __newindex (table) */
+#endif
 } Table;
 
 
diff --git a/src/ltable.c b/patched/src/ltable.c
--- a/src/ltable.c
+++ b/src/ltable.c
@@ -372,6 +372,11 @@ Table *luaH_new (lua_State *L) {
   t->array = NULL;
   t->sizearray = 0;
   setnodevector(L, t, 0);
+#if ZLUA_FAST_METATABLE
+  t->zlua_mt_kind = 0;
+  setnilvalue(&t->zlua_index);
+  setnilvalue(&t->zlua_newindex);
+#endif
   return t;
 }
 
diff --git a/src/luaconf.h b/patched/src/luaconf.h
--- a/src/luaconf.h
+++ b/src/luaconf.h
@@ -431,7 +431,7 @@
 */
 
 /* the following operations need the math library */
-#if defined(lobject_c) || defined(lvm_c)
+#if 1 /* ZLua: Il2Cpp lump-safe - always define luai_nummod/pow */
 #include <math.h>
 #define luai_nummod(L,a,b)	((a) - l_mathop(floor)((a)/(b))*(b))
 #define luai_numpow(L,a,b)	(l_mathop(pow)(a,b))
@@ -544,8 +544,22 @@
 ** Local configuration. You can use this space to add your redefinitions
 ** without modifying the main part of the file.
 */
+/* ZLua: Apple mobile - system(3) is unavailable on iOS/tvOS/watchOS. */
+#if defined(__APPLE__)
+#include <TargetConditionals.h>
+#if TARGET_OS_IPHONE
+#if !defined(LUA_USE_IOS)
+#define LUA_USE_IOS
+#endif
+#include <stdlib.h>
+#undef system
+#define system(s) ((s) == NULL ? 1 : -1)
+#endif
+#endif
 
-
-
+/* ZLua: FastMT enabled for Lua 5.2 (gettable/settable path). */
+#if !defined(ZLUA_FAST_METATABLE)
+#define ZLUA_FAST_METATABLE 1
 #endif
 
+#endif
diff --git a/src/lvm.c b/patched/src/lvm.c
--- a/src/lvm.c
+++ b/src/lvm.c
@@ -25,6 +25,7 @@
 #include "ltable.h"
 #include "ltm.h"
 #include "lvm.h"
+#include "zlua_fastmt.h"
 
 
 
@@ -120,9 +121,52 @@ void luaV_gettable (lua_State *L, const TValue *t, TValue *key, StkId val) {
         return;
       }
       /* else will try the tag method */
+#if ZLUA_FAST_METATABLE
+      /* After raw miss: sealed SMT (often setmetatable(T,T)) serves memberTable. */
+      if (h->metatable != NULL && h->metatable->zlua_mt_kind != 0) {
+        Table *mt = h->metatable;
+        Table *idx = hvalue(&mt->zlua_index);
+        const TValue *slotv = luaH_get(idx, key);
+        if (!ttisnil(slotv)) {
+          if (zlua_is_getter(slotv))
+            zlua_vm_call_getter(L, slotv, t, val);
+          else
+            setobj2s(L, val, slotv);
+          return;
+        }
+        /* Static type table: extras such as _default live on SMT itself. */
+        {
+          const TValue *extra = luaH_get(mt, key);
+          if (!ttisnil(extra)) {
+            setobj2s(L, val, extra);
+            return;
+          }
+        }
+        setnilvalue(val);
+        return;
+      }
+#endif
     }
     else if (ttisnil(tm = luaT_gettmbyobj(L, t, TM_INDEX)))
       luaG_typeerror(L, t, "index");
+#if ZLUA_FAST_METATABLE
+    else if (ttisuserdata(t)) {
+      Table *mt = uvalue(t)->metatable;
+      if (mt != NULL && mt->zlua_mt_kind != 0) {
+        Table *idx = hvalue(&mt->zlua_index);
+        const TValue *slotv = luaH_get(idx, key);
+        if (!ttisnil(slotv)) {
+          if (zlua_is_getter(slotv))
+            zlua_vm_call_getter(L, slotv, t, val);
+          else
+            setobj2s(L, val, slotv);
+          return;
+        }
+        setnilvalue(val);
+        return;
+      }
+    }
+#endif
     if (ttisfunction(tm)) {
       callTM(L, tm, t, key, val, 1);
       return;
@@ -137,6 +181,37 @@ void luaV_settable (lua_State *L, const TValue *t, TValue *key, StkId val) {
   int loop;
   for (loop = 0; loop < MAXTAGLOOP; loop++) {
     const TValue *tm;
+#if ZLUA_FAST_METATABLE
+    {
+      Table *mt = NULL;
+      if (ttisuserdata(t))
+        mt = uvalue(t)->metatable;
+      else if (ttistable(t))
+        mt = hvalue(t)->metatable;
+      if (mt != NULL && mt->zlua_mt_kind != 0) {
+        /* Sealed ZLua mt: allow in-place update of existing table keys, else setters. */
+        if (ttistable(t)) {
+          Table *h = hvalue(t);
+          TValue *oldval = cast(TValue *, luaH_get(h, key));
+          if (!ttisnil(oldval)) {
+            setobj2t(L, oldval, val);
+            invalidateTMcache(h);
+            luaC_barrierback(L, obj2gco(h), val);
+            return;
+          }
+        }
+        {
+          Table *setters = hvalue(&mt->zlua_newindex);
+          const TValue *slotv = luaH_get(setters, key);
+          if (!ttisnil(slotv) && zlua_is_setter(slotv)) {
+            zlua_vm_call_setter(L, slotv, t, val);
+            return;
+          }
+        }
+        luaG_runerror(L, "zlua: member not found or read-only");
+      }
+    }
+#endif
     if (ttistable(t)) {  /* `t' is a table? */
       Table *h = hvalue(t);
       TValue *oldval = cast(TValue *, luaH_get(h, key));
diff --git a/patched/src/zlua_fastmt.c b/patched/src/zlua_fastmt.c
new file mode 100644
--- /dev/null
+++ b/src/zlua_fastmt.c
@@ -0,0 +1,75 @@
+/*
+** ZLua sealed-metatable support for PUC-Rio Lua 5.2 VM.
+*/
+#define LUA_CORE
+
+#include "lua.h"
+#include "lauxlib.h"
+
+#include "lapi.h"
+#include "ldo.h"
+#include "lfunc.h"
+#include "lgc.h"
+#include "lobject.h"
+#include "lstate.h"
+#include "ltable.h"
+#include "ltm.h"
+#include "zlua_fastmt.h"
+
+#if ZLUA_FAST_METATABLE
+
+TValue *zlua_index2value (lua_State *L, int idx);
+
+LUA_API void zlua_mt_seal (lua_State *L, int mtIndex) {
+  Table *mt;
+  const TValue *idx;
+  const TValue *nidx;
+  TValue *o;
+  lua_lock(L);
+  o = zlua_index2value(L, mtIndex);
+  api_check(L, ttistable(o), "table expected");
+  mt = hvalue(o);
+  idx = luaH_getstr(mt, G(L)->tmname[TM_INDEX]);
+  nidx = luaH_getstr(mt, G(L)->tmname[TM_NEWINDEX]);
+  if (ttisnil(idx) || !ttistable(idx)) {
+    lua_unlock(L);
+    luaL_error(L, "zlua_mt_seal: __index must be a table");
+  }
+  if (ttisnil(nidx) || !ttistable(nidx)) {
+    lua_unlock(L);
+    luaL_error(L, "zlua_mt_seal: __newindex must be a table");
+  }
+  setobj(L, &mt->zlua_index, idx);
+  setobj(L, &mt->zlua_newindex, nidx);
+  luaC_barrierback(L, obj2gco(mt), idx);
+  luaC_barrierback(L, obj2gco(mt), nidx);
+  mt->zlua_mt_kind = 1;
+  lua_unlock(L);
+}
+
+
+LUA_API void zlua_vm_call_getter (lua_State *L, const TValue *f,
+                                  const TValue *obj, StkId res) {
+  ptrdiff_t result = savestack(L, res);
+  StkId func = L->top;
+  setobj2s(L, func, f);
+  setobj2s(L, func + 1, obj);
+  L->top += 2;
+  /* 5.2: luaD_call(..., allowyield); no luaD_callnoyield */
+  luaD_call(L, func, 1, isLua(L->ci));
+  res = restorestack(L, result);
+  setobjs2s(L, res, --L->top);
+}
+
+
+LUA_API void zlua_vm_call_setter (lua_State *L, const TValue *f,
+                                  const TValue *obj, StkId val) {
+  StkId func = L->top;
+  setobj2s(L, func, f);
+  setobj2s(L, func + 1, obj);
+  setobj2s(L, func + 2, val);
+  L->top += 3;
+  luaD_call(L, func, 0, isLua(L->ci));
+}
+
+#endif /* ZLUA_FAST_METATABLE */
diff --git a/patched/src/zlua_fastmt.h b/patched/src/zlua_fastmt.h
new file mode 100644
--- /dev/null
+++ b/src/zlua_fastmt.h
@@ -0,0 +1,32 @@
+/*
+** ZLua sealed-metatable helpers (FastMT) for PUC-Rio Lua 5.2.
+** Active when ZLUA_FAST_METATABLE != 0 (luaconf.h).
+*/
+#ifndef zlua_fastmt_h
+#define zlua_fastmt_h
+
+#include "luaconf.h"
+#include "lua.h"
+#include "lobject.h"
+
+#if ZLUA_FAST_METATABLE
+
+LUA_API int zlua_index_getter (lua_State *L);  /* (obj) -> value */
+LUA_API int zlua_index_setter (lua_State *L);  /* (obj, value) -> 0 */
+
+LUA_API void zlua_mt_seal (lua_State *L, int mtIndex);
+
+LUA_API void zlua_vm_call_getter (lua_State *L, const TValue *f,
+                                  const TValue *obj, StkId res);
+LUA_API void zlua_vm_call_setter (lua_State *L, const TValue *f,
+                                  const TValue *obj, StkId val);
+
+#define zlua_is_getter(o) \
+  (ttisCclosure(o) && clCvalue(o)->f == zlua_index_getter)
+
+#define zlua_is_setter(o) \
+  (ttisCclosure(o) && clCvalue(o)->f == zlua_index_setter)
+
+#endif /* ZLUA_FAST_METATABLE */
+
+#endif
